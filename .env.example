# Database
MONGODB_CNN=mongodb://your-mongodb-connection-string

# Server
PORT=8080

# Email Configuration
# Provider options: 'resend' or 'ses'
EMAIL_PROVIDER=resend
SENDER_EMAIL=melissa@theriseupculture.com

# Resend Configuration (if EMAIL_PROVIDER=resend)
RESEND_API_KEY=re_your_resend_api_key

# AWS SES Configuration (if EMAIL_PROVIDER=ses)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
